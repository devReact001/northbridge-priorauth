# Where the FastAPI backend runs. Used only by the Next.js server, never sent to the browser.
API_BASE_URL=http://localhost:8000
# Must match API_KEY in the backend .env. Leave empty if the backend has no API_KEY (localhost only).
API_KEY=
